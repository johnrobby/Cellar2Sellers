<div class="tab-pane fade" id="list-privateStock" role="tabpanel" aria-labelledby="list-privateStock-list">
    <div class="container">
        <div class="container">
            <div class="row">
                <div class="col text-dark">    
                {{#each wines}}
                    {{#if sale}}
                    {{> wines/wine_card/card sale=false}}
                    {{/if}}
                {{/each}}                  
       
                </div>
                <div class="col text-dark">
                {{#each wines}}
                    {{#if sale}}
                    {{> wines/wine_card/card sale=false}}
                    {{/if}}
                {{/each}}                  
                </div>
            </div>
            <br>
        </div>
    </div>
</div>